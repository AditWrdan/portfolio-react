VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — used by scripts/seed.mjs. Never prefix with VITE_
# (a VITE_ prefix would bundle it into the public client JS).
SUPABASE_SERVICE_ROLE_KEY=
